services:
  - type: web
    name: monchat-backend
    env: python
    buildCommand: pip install -r requirements.txt && python nltk_setup.py
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12 # Stable version for TF
